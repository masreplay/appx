import 'package:injectable/injectable.dart';

const String productionName = "production";
const String mockName = "mock";

const Named productionService = Named(productionName);
const Named mockService = Named(mockName);
